# Stripe API Key
STRIPE_SECRET_KEY=sk_live_your_key_here

# Authentication secret - Vercel backend must send this as Bearer token
# Generate with: openssl rand -hex 32
RELAY_SECRET=your_random_secret_here

# Port (Railway will set this automatically)
PORT=3001
